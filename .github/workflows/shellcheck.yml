name: ShellCheck
on:
  push:
    paths:
      - 'macos/src/build/**'
      - '.github/workflows/shellcheck.yml'
  pull_request:
  workflow_dispatch:
jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run ShellCheck on installer scripts
        run: |
          shellcheck --version
          shellcheck macos/src/build/build.sh macos/src/build/scripts/preinstall macos/src/build/scripts/postinstall
